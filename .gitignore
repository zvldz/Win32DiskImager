Debug/
Release/
build*/
*.exe
*.pro.user*
*dll
*qm

# Local build outputs, deployed runtime files, debug launcher.
# CI produces artifacts under Output/ and artifacts/ (ignored too).
bin/
Output/
artifacts/

# Ad-hoc local utilities (one-shot translator scripts, disk-probe debug
# binaries, etc). Add with `git add -f tools/<file>` if a particular tool
# is worth tracking long-term.
tools/

# Editor/IDE and agent metadata.
.claude/
.vscode/
.idea/
CLAUDE.md

# Ad-hoc working notes (session handoffs, debug summaries). Delete or
# rename to commit if any one becomes worth tracking.
temp_*.md
