Work list (inherited from upstream):

Features:
    Compression support   -- DONE in 2.2.0: .gz / .xz on Write + Verify
                             (GUI + CLI, streaming decode, MT xz decoder)
    Reformat device       -- not implemented. Bringing an SD card back to
                             a clean FAT32 after it has been written with
                             a Linux / Pi image. Low priority; SDA Card
                             Formatter and the Windows Disk Management
                             snap-in already cover this.
